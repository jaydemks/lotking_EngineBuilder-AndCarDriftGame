# Release Notes

## v0.5.2 — Next editor features

### Release Status

- Status: in progress.
- Scope: new editor features and focused fixes after the `v0.5.1` refactor release.

### Planned / Candidate Work

- Add editor/game input work for controllers where browser APIs allow it, including USB gamepads.
- Continue material workflow improvements for imported and built-in objects.
- Keep improving playable export, project export, and editor/runtime separation.
- Continue small, safe cleanup only when it directly supports new features or fixes.

### Notes

- Previous cumulative history and the `v0.5.1` refactor release are now archived in `docs/releases/v0.5.1.md`.
- The earlier Git-ready beta release note is in `docs/releases/v0.5.0-beta.md`.
